apiVersion: v1
kind: Service
metadata: 
 name: nginx
 labels:
  purpose: service-demo
spec:
 selector:
  project: roboshop
  env: dev
 ports:
 - protocol: TCP
   port: 80
   targetPort: 80

---

apiVersion: v1
kind: Pod
metadata:
 name: svc-demo
 labels:
  project: roboshop
  env: dev
spec:
 containers:
 - name: nginx
   image: nginx